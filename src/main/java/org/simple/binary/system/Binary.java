package org.simple.binary.system;

/**
 * This interface represents a binary thing top level. This interface doesn't extends some
 * interface and is used like a marker for other binary things.
 */
public interface Binary {
}
